push(data) = inserts data onto stacks

pop() = deleted the last inserted element from the stack

top() = returns the last inserted element without removing it.

isEmpty() = returns TRUE if the stack is empty, else return FALSE.

isFull() = returnd TRUE is the stack is full, else returns FALSE.

peek() = it can tell the index of any element in a stack.